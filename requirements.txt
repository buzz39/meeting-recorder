# Core
pyaudiowpatch>=0.2.12,<1.0
faster-whisper>=1.0.0,<2.0
numpy>=1.24.0,<3.0

# Speaker diarization (full) — pyannote requires HF_TOKEN and ~2GB for torch
# Pinned to the 3.x series; pyannote regularly ships breaking pipeline changes.
pyannote.audio>=3.1.0,<4.0
torch>=2.0.0,<3.0

# System tray (Windows)
pystray>=0.19.0,<1.0
Pillow>=10.0.0,<12.0
